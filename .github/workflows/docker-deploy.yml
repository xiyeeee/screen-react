name: Build and Push Docker Image

on:
  push:
    tags:
      - 'v*.*.*'  # 当推送版本标签时触发

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Aliyun Container Registry
      run: |
        echo "${{ secrets.ALIYUN_PASSWORD }}" | docker login --username ${{ secrets.ALIYUN_USERNAME }} --password-stdin crpi-ow7mi2apt624v3c8.cn-heyuan.personal.cr.aliyuncs.com

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: crpi-ow7mi2apt624v3c8.cn-heyuan.personal.cr.aliyuncs.com/xiye-docker/screen-react
        tags: |
          type=ref,event=tag

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
